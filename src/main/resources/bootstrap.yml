spring:
  data:
    mongodb:
      uri: mongodb://localhost:27026/yanki-transfers
  application:
    name: yanki-transfers-service
  cloud:
    config:
      #enabled: true
      uri: http://localhost:8081